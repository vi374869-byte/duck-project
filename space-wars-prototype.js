const canvas=document.getElementById('game'),ctx=canvas.getContext('2d'),scoreEl=document.getElementById('score'),bestEl=document.getElementById('best');let ship={x:450,y:480},shots=[],rocks=[],score=0,keys={},last=0,spawn=0,best=+localStorage.getItem('spaceWarsBest')||0;bestEl.textContent=best;const stars=Array.from({length:90},()=>({x:Math.random()*900,y:Math.random()*540,r:Math.random()*2+1}));
function fire(){if(shots.length<5)shots.push({x:ship.x,y:ship.y-28})}function move(amount){ship.x=Math.max(30,Math.min(870,ship.x+amount))}window.addEventListener('keydown',e=>{keys[e.key]=true;if(e.key===' ')fire()});window.addEventListener('keyup',e=>keys[e.key]=false);[['left',-42],['right',42]].forEach(([id,n])=>document.getElementById(id).addEventListener('pointerdown',()=>move(n)));document.getElementById('shoot').addEventListener('pointerdown',fire);canvas.addEventListener('pointerdown',e=>{const r=canvas.getBoundingClientRect();ship.x=(e.clientX-r.left)/r.width*900;fire()});
function reset(){rocks=[];shots=[];score=0;scoreEl.textContent=score;ship.x=450}function tick(t){const dt=Math.min(.04,(t-last)/1000||0);last=t;if(keys.ArrowLeft||keys.a)move(-360*dt);if(keys.ArrowRight||keys.d)move(360*dt);spawn-=dt;if(spawn<=0){rocks.push({x:35+Math.random()*830,y:-30,r:16+Math.random()*20,v:100+Math.random()*100});spawn=.65}shots.forEach(s=>s.y-=520*dt);rocks.forEach(r=>r.y+=r.v*dt);shots=shots.filter(s=>s.y>-20);for(let i=rocks.length-1;i>=0;i--){const r=rocks[i];if(Math.hypot(ship.x-r.x,ship.y-r.y)<r.r+24){reset();break}for(let j=shots.length-1;j>=0;j--)if(Math.hypot(shots[j].x-r.x,shots[j].y-r.y)<r.r+7){rocks.splice(i,1);shots.splice(j,1);score+=10;window.Arcade?.earn(1);scoreEl.textContent=score;if(score>best){best=score;bestEl.textContent=best;localStorage.setItem('spaceWarsBest',best)}break}}rocks=rocks.filter(r=>r.y<580);draw();requestAnimationFrame(tick)}function draw(){ctx.clearRect(0,0,900,540);stars.forEach(s=>{ctx.fillStyle='#fff';ctx.globalAlpha=.45;ctx.fillRect(s.x,s.y,s.r,s.r)});ctx.globalAlpha=1;rocks.forEach(r=>{ctx.fillStyle='#9ba4ca';ctx.beginPath();ctx.arc(r.x,r.y,r.r,0,7);ctx.fill();ctx.fillStyle='#60688c';ctx.beginPath();ctx.arc(r.x-6,r.y-4,r.r/4,0,7);ctx.fill()});ctx.fillStyle='#fff69a';shots.forEach(s=>ctx.fillRect(s.x-3,s.y,6,16));ctx.save();ctx.translate(ship.x,ship.y);ctx.fillStyle='#6ff3ff';ctx.beginPath();ctx.moveTo(0,-30);ctx.lineTo(25,25);ctx.lineTo(0,15);ctx.lineTo(-25,25);ctx.closePath();ctx.fill();ctx.fillStyle='#ff719b';ctx.fillRect(-7,10,14,15);ctx.restore()}requestAnimationFrame(tick);
